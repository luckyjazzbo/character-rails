<% config = @object.admin_form_config %>

<%= simple_form_for @object, url: config[:url] do |f| %>
  <% config[:fields].each do |name| %>
    <%= f.input name %>
  <% end %>

  <%= f.button :submit %>
<% end %>