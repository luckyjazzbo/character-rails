<% set_meta_tags  title:        @page.title,
                  description:  @page.description,
                  keywords:     @page.keywords,
                  canonical:    "http://#{ ENV['WEBSITE_URL'] }#{ @page.permalink }",
                  open_graph:   { type:        'article',
                                  url:         "http://#{ ENV['WEBSITE_URL'] }#{ @page.permalink }",
                                  site_name:   ENV['WEBSITE_NAME'],
                                  image:       @page.featured_image_url,
                                  title:       @page.title,
                                  description: @page.description,
                                } %>

<%= @page.html.html_safe %>
